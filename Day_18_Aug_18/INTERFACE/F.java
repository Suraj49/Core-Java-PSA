package Day_18_Aug_18.INTERFACE;

public interface F {
    int id=20;
    final static int id2=110; //both variable have same nature
    //if we make variable inside interface it is final and static by default
    // int i; error because it is by default final and we know that initialization of final variables'  req.
}

